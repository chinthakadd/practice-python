def fn(arr):
    left = ans = curr = 0

    for right in range(len(arr)):
        # TODO: do logic here to add arr[right] to curr
        while WINDOW_CONDITION_BROKEN:
            # TODO: remove arr[left] from curr
            left += 1

        # TODO: update ans

    return ans